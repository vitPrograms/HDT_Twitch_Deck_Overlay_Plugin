<UserControl x:Class="TwitchDeckOverlay.UI.CardTooltip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="200">
    <Border Background="#2D2D30" 
            BorderBrush="#3F3F46" 
            BorderThickness="1" 
            CornerRadius="5"
            Padding="10">
        <StackPanel>
            <!-- Назва карти -->
            <TextBlock Text="{Binding Name}" 
                       FontWeight="Bold" 
                       FontSize="14"
                       Foreground="White"
                       Margin="0,0,0,8"/>
            
            <!-- Картинка карти (якщо доступна) -->
            <Image Source="{Binding ImageUrl}" 
                   Width="150" 
                   Height="200"
                   Stretch="Uniform"
                   Margin="0,0,0,8"
                   Visibility="{Binding ImageUrl, Converter={StaticResource NullToVisibilityConverter}}"/>
            
            <!-- Основна інформація -->
            <Grid Margin="0,0,0,5">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <TextBlock Grid.Row="0" Grid.Column="0" Text="Mana: " FontWeight="Bold" Foreground="LightBlue"/>
                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding Cost}" Foreground="White"/>
                
                <TextBlock Grid.Row="1" Grid.Column="0" Text="Attack: " FontWeight="Bold" Foreground="Orange"/>
                <TextBlock Grid.Row="1" Grid.Column="1" Text="{Binding Attack}" Foreground="White"/>
                
                <TextBlock Grid.Row="2" Grid.Column="0" Text="Health: " FontWeight="Bold" Foreground="Red"/>
                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding Health}" Foreground="White"/>
                
                <TextBlock Grid.Row="3" Grid.Column="0" Text="Count: " FontWeight="Bold" Foreground="Yellow"/>
                <TextBlock Grid.Row="3" Grid.Column="1" Text="{Binding Count}" Foreground="White"/>
            </Grid>
            
            <!-- Опис карти -->
            <TextBlock Text="{Binding Text}" 
                       TextWrapping="Wrap"
                       MaxWidth="180"
                       Foreground="LightGray"
                       FontSize="11"
                       Margin="0,5,0,0"/>
        </StackPanel>
    </Border>
</UserControl>